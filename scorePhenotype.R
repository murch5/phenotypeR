#
# SCORE PHENOTYPE FUNCTION
#
#
# Function used to score individual phenotypes according to passed weightings
#
# - passes data.frame containing subset of phenotypic information to be score, data.frame containing columns names and associated scoring functions for each)
#   and data.frame containing scoring groupings to be applied to calculated different total scores
# -creates various scoring columns according to arguments passed
# - returns data.frame with new score columns
#
# arguments:
#   dataSet - data.frame containing subset of phenotypic data to be analyzed
#   
#   
#
# return:
#   output - data.frame containing new score values


scorePhenotype <- function(input,scoringFuncMapping,scoringGrouping)
{
  
  return(output)
}